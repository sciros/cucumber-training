require 'rake'

load 'scripts/rake_tasks/arg_extractor_task.rb'
load 'scripts/rake_tasks/local_rake_tasks.rb'

# aliases for convenience
task :cucumber => 'local:single_run'
task :manual => 'local:manual_run'
task :default => ':cucumber'
